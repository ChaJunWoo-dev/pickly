import { EmptyState, Screen } from '@/components';

export default function RewardsTabScreen() {
  return (
    <Screen centered>
      <EmptyState description="포인트 내역과 리워드 관련 화면" title="리워드" />
    </Screen>
  );
}
