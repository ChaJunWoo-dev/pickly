import { EmptyState, Screen } from '@/components';

export default function ExploreScreen() {
  return (
    <Screen centered>
      <EmptyState
        description="포인트 상점과 랭킹 화면은 홈 피드 다음에 이어서 만들 예정이에요."
        title="준비 중"
      />
    </Screen>
  );
}
